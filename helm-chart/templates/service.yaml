apiVersion: v1
kind: Service
metadata:
  name: {{ include "safe-client-gateway-helm.fullname" . }}
  labels:
    {{- include "safe-client-gateway-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "safe-client-gateway-helm.selectorLabels" . | nindent 4 }}
